[package]
name = "ferrocarril-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
phonesis = { path = "../phonesis", features = ["english"] }
memmap2 = "0.5"

[features]
default = ["weights"]
weights = []